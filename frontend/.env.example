# 프론트엔드는 보통 환경변수가 필요 없습니다.
# (vercel.json 의 rewrite 가 /api 요청을 백엔드로 넘겨주기 때문)
#
# 만약 rewrite 대신 백엔드 주소를 직접 쓰고 싶다면 아래를 채우세요.
# 이 경우 백엔드의 FRONTEND_ORIGINS 에 이 사이트 주소를 꼭 넣어야 합니다(CORS).
# VITE_API_BASE=https://mmz-scheduler-api.onrender.com/api
