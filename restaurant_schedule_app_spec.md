# 식당 직원 스케줄링 앱 — 스펙 문서

## 1. 앱 개요
사장님이 매달 식당 직원 근무 스케줄을 쉽게 만들고, 완성된 스케줄을 QR 코드로 직원들에게 공유하는 앱.
- 사용자: 식당 사장님 (관리자), 직원 (조회 전용)
- 핵심 가치: 복잡한 조건(포지션, 고용형태, 연차, 최소 인원)을 반영한 **자동 스케줄 배치**
- QR 코드는 완성된 스케줄을 전달하는 수단일 뿐, 핵심 기능은 자동배치 엔진

---

## 2. 사용자 및 데이터 모델

### 2.1 직원 (Staff)
| 필드 | 설명 |
|---|---|
| 이름 | |
| 고용형태 | 정직원 / 파트타임 |
| 포지션 | 홀 전담 / 주방 전담 / 겸직(홀+주방) |
| 역할 | 일반 직원 / 사장님 / 점장 |
| 연차 정보 | 기본휴무, 전월잔여연차, 전월발생연차, 합연차, 연차사용, 잔여연차 (자동 계산) |

### 2.2 사장님 특이사항
- 사장님도 근무 풀에 포함되어 직접 근무 배치됨 (근무 코드 `&&`)
- 사장님 2인:
  - A: 홀만 가능 (포지션: 홀 전담)
  - B: 홀 + 주방 모두 가능 (포지션: 겸직)
- 사장님이 근무하는 날은 **시간대가 항상 "마감" 고정** (오픈/미들 배치 안 함)
- 겸직 가능한 사장님(B)도 **가능하면 홀(FC) 우선 배치**, 주방(BC)은 홀 인원이 이미 충분하거나 주방 인원이 부족할 때만 보조적으로 배치
- 사장님은 **최대한 주말 근무를 배치하지 않음** (우선순위 낮게, 평일 우선 투입)
- 사장님은 직원 간 "공정성" 로직 대상에서 제외

### 2.3 점장 특이사항
- 근무 조건(고용형태, 연차/휴무 계산)은 **일반 직원(정직원)과 동일**하게 관리
- 포지션 구분은 있으나(홀/주방/겸직), 근무하는 날은 **사장님과 동일하게 항상 "마감" 고정**
- 공정성(마감 몰아주기 방지) 로직 대상 포함 여부는 추후 결정 필요 (기본값: 사장님처럼 제외 — 마감 고정이라 어차피 몰릴 수밖에 없음)

### 2.4 "관리 책임자 최소 1인 출근" 규칙 ⭐
- 사장님 2인 + 점장 1인 = **"관리 책임자" 그룹 (총 3인)**
- **매일 이 3인 중 최소 1명은 반드시 출근**해야 함 (자동배치 시 하드 제약조건 — 위반 불가)
- 3인 모두 쉬는 날이 발생하지 않도록 자동배치 단계에서 우선 체크
- 이 제약이 다른 제약(사장님 주말 최소화 등)과 충돌할 경우, **"최소 1인 출근"이 우선** (주말이라도 관리 책임자 중 누군가는 출근해야 함)

### 2.5 근무 코드 (예시, 사장님이 커스터마이징 가능해야 함)

홀/주방 구분이 코드 자체에 드러나도록 재정의. 겸직 직원도 그날 실제 배정된 포지션이 코드로 바로 보이므로 별도 표기 없이도 홀/주방 구분이 명확해짐.

| 코드 | 의미 | 시간 |
|---|---|---|
| FO | 홀오픈 (Front Open) | 10:45~20:45 |
| FC | 홀마감 (Front Close) | 11:30~21:30 |
| BO | 주방오픈 (Back Open) | 10:30~20:30 |
| BM | 주방미들 (Back Middle) | 11:00~21:00 |
| BC | 주방마감 (Back Close) | 11:30~21:30 |
| 풀오마 | 파트타임 고정 근무 (하루 종일) | 10:30~21:30 |
| D/O | 휴무 | - |
| 연차 | 연차 사용 | - |
| 사전휴무 | 사전 휴무 신청 (월 최대 4일, 근무일수 유지) | - |

- **겸직(홀+주방) 직원**: 그날 홀에 배정되면 FO/FC, 주방에 배정되면 BO/BM/BC로 표시 → 코드만 봐도 그날 어느 포지션으로 일하는지 바로 구분됨
- **사장님/점장**: 항상 마감 고정이므로 포지션에 따라 FC 또는 BC로 표시 (별도의 `&&` 코드 없이도, 직원 이름 행 자체로 사장님/점장 구분 가능하므로 코드는 일반 마감 코드와 동일하게 사용)

---

## 3. 연차/휴무 관리
- 직원별 기본휴무, 전월잔여연차, 전월발생연차 → 합연차 자동 계산
- 연차사용 반영 → 잔여연차 자동 갱신
- 직원이 **원하는 연차 날짜를 미리 신청**할 수 있어야 함
- 신청된 연차는 자동배치 시 **최우선으로 고정** (해당 직원 그 날짜 배치 제외, **총 근무일수도 그만큼 줄어듦**)

### 3.1 사전 휴무 신청 (연차와 별개 개념) ⭐
- 직원이 스케줄 생성 **전에 월 최대 4일까지** "이 날짜는 근무 불가"를 미리 신청 가능
- 연차와의 차이:
  - **연차**: 그날 쉬고, 총 근무일수도 줄어듦 (잔여연차 차감)
  - **사전 휴무 신청**: 그날은 배치에서 제외되지만, **총 근무일수는 그대로 유지** — 자동배치가 그만큼을 다른 날짜에 배정해서 채움 (연차 차감 없음)
- 자동배치 시 연차와 동일하게 **하드 제약(해당 날짜 배치 금지)**으로 취급하되, 근무일수 계산 로직에서는 연차와 분리해서 처리해야 함
- 월 4일 초과 신청 시 서버에서 거부 (또는 사장님 승인 필요하도록 추후 확장 가능)

---

## 4. 인원 조건 (자동배치 제약)

필요 인원은 "포지션 × 시간대" 단위로 세분화해서 설정한다 (홀/주방 뭉뚱그린 숫자가 아니라, 오픈/미들/마감 각각 몇 명 필요한지 지정).

### 4.1 이 식당의 기본값 (예시, 설정 화면에서 사장님이 직접 수정 가능해야 함)
| 포지션 | 오픈 | 미들 | 마감 |
|---|---|---|---|
| 홀 | 1명 | - | 1명 |
| 주방 | 1명 | 1명 | 2명 |

- 홀은 미들 시간대 없음 (오픈 1 + 마감 1만 필요)
- 주방은 오픈/미들/마감 모두 필요 (오픈 1 + 미들 1 + 마감 2)

### 4.2 고용형태별 근무 방식
- **파트타임 직원**: 시간대 구분 없이 **항상 "풀오마"(풀타임 오픈~마감) 고정 근무**로 배치. 오픈/미들/마감 개별 슬롯이 아니라 하루 전체를 커버하는 형태.
- **정직원**: 오픈/미들/마감 중 하나로 배치되며, 포지션(홀/주방/겸직)에 맞는 시간대에만 배치 가능
- **사장님**: 근무 시 항상 "마감" 고정 (2.2 참고)

### 4.3 공통 규칙
- 요일별로 필요 인원이 다르게 설정될 수도 있음 (기본값은 매일 동일, 특정 요일만 다르게 오버라이드 가능)
- 직원의 포지션(홀/주방/겸직)에 맞는 슬롯에만 배치

---

## 5. 자동배치 로직 (우선순위 순서)

1. **연차 고정 + 사전 휴무 신청 고정**: 신청된 연차 날짜 및 사전 휴무 신청 날짜(월 최대 4일)는 해당 직원 배제하고 시작 (단, 사전 휴무 신청은 총 근무일수를 줄이지 않고 다른 날짜로 이동시켜 채움)
2. **관리 책임자 최소 1인 출근 (하드 제약)**: 사장님 2인 + 점장 1인 중 매일 최소 1명은 반드시 출근 (2.4 참고, 다른 제약보다 우선)
3. **필요 인원 채우기**: 요일/시간대별 포지션×시간대 최소 인원 충족 (4번 참고)
4. **포지션 매칭**: 겸직 직원은 홀/주방 어디든 배치 가능, 전담 직원은 해당 포지션만
5. **사장님/점장 배치**: 근무 시 항상 "마감" 고정, 사장님은 주말 근무 최소화(단, 2번 제약 위반 시엔 주말이라도 투입)
6. **공정성 (일반 직원만 해당, 사장님/점장 제외)**: 마감/오픈 몰아주기 방지, 근무 부담 최대한 공평하게 분산
6-1. **연속 근무일수 제한**: 가능하면 **6일 이상 연속 근무가 발생하지 않도록** 배치 (소프트 제약 — 인원 부족 등으로 불가피하면 허용하되, 최대한 피하는 방향으로 우선순위를 둠)
7. **인원 부족 시**: 자동배치를 중단하지 않고 최대한 채운 뒤, 부족한 슬롯은 사장님에게 **경고 표시**

### 기술 접근
- 이 문제는 "제약 충족 최적화(Constraint Satisfaction/Optimization)" 문제
- **Google OR-Tools (CP-SAT solver)** 같은 무료 오픈소스 라이브러리로 구현 권장
- 규칙(연차, 인원수, 포지션, 공정성, 사장님 주말 회피)을 제약조건으로 입력 → 솔버가 최적 스케줄 계산
- 완전히 못 채우는 경우 infeasible 처리 대신 부분 해를 찾아 경고로 표시

---

## 6. 화면 구성

### 6.1 사장님용 (관리자)
- 직원 등록/관리 (이름, 고용형태, 포지션)
- 연차 신청 확인 (직원이 신청한 연차 목록)
- 요일/시간대별 필요 인원 설정
- 자동배치 실행 → 결과 확인
- 결과 수동 수정 (드래그 또는 셀 클릭으로 변경)
- 완성된 스케줄 QR 코드 생성

### 6.2 직원용 (조회 전용)
- 로그인 없이 QR 스캔 → 웹페이지 바로 접속
- 이미지 형태로 스케줄표 확인
- (선택) 본인 이름/근무 칸 하이라이트 표시

---

## 7. QR 공유 흐름
1. 사장님이 스케줄 완성 → "공유" 버튼
2. 서버가 해당 스케줄에 대한 고유 URL 생성 (예: `https://앱주소/schedule/{고유코드}`)
3. URL을 QR 코드로 변환하여 화면에 표시 (또는 출력/카톡 공유)
4. 직원이 QR 스캔 → 브라우저에서 이미지 형태 스케줄 바로 조회

---

## 8. MVP 범위 제안
1. 직원 등록 + 연차 정보 관리
2. 연차 신청 기능
3. 요일/시간대별 필요 인원 설정
4. 자동배치 엔진 (OR-Tools 기반)
5. 배치 결과 수동 수정 화면
6. QR 코드 생성 + 직원 조회 페이지 (이미지 형태)

## 9. 향후 확장 가능 아이디어 (MVP 이후)
- 직원 대타 요청/교환 기능
- 급여 계산 연동 (근무시간 기반)
- 푸시 알림 (스케줄 변경 시 직원에게 알림)
- 여러 매장 관리 (사장님이 매장 여러 개 운영 시)

---

## 10. 멀티 매장(범용화) 대비 설계

지금은 "이 식당 하나" 기준으로 만들지만, 나중에 여러 식당/사장님이 쓰는 서비스로 확장할 가능성을 염두에 두고 처음부터 아래 구조로 설계한다.

### 10.1 매장(Tenant) 개념 도입
- 모든 데이터(직원, 스케줄, 연차, 근무코드, 인원조건)는 특정 "매장(store)"에 종속되도록 설계
- 예: `staff` 테이블에 `store_id` 컬럼 필수 포함
- 지금은 매장이 1개뿐이지만, DB/데이터 구조 자체는 "매장 1개 = 하나의 레코드"로 시작

### 10.2 근무 코드 / 포지션 하드코딩 금지
- O, M, C, 홀오픈, 홀마감, 풀오픈 등은 **이 식당 전용 설정값**으로 취급
- 사장님이 "설정" 화면에서 근무 코드(이름, 시간대, 색상)와 포지션(홀/주방 등)을 직접 정의·수정할 수 있게 구현
- 업종이 다른 매장(카페, 술집 등)도 동일한 앱 구조를 재사용 가능해짐

### 10.3 계정/인증 구조
- 지금 MVP는 로그인 없이 사장님 1명이 관리자 화면 접근해도 되지만
- 추후 확장을 고려해 "사장님 계정(user) ↔ 매장(store)" 1:N 관계로 설계해두면 나중에 회원가입/멀티 매장 추가가 수월함

### 10.4 지금 단계에서는 하지 않아도 되는 것
- 회원가입/로그인 화면 (MVP는 단일 사장님이라 생략 가능)
- 요금제/결제 로직
- 매장 간 데이터 완전 격리 (아직 매장이 1개이므로 우선순위 낮음)

> 요약: **지금은 1개 매장 기준으로 만들되, 데이터 구조와 설정 가능한 항목(근무코드/포지션)만 "매장별로 다를 수 있다"는 전제로 설계해두면 나중에 범용 서비스로 확장할 때 구조를 갈아엎지 않아도 된다.**
